import flatpickr from "flatpickr";

// run code on load
document.addEventListener("DOMContentLoaded", () => {
  console.log("init-flatpickr.ts - page loaded!");

  flatpickr(".flatpickr-demo", {});
});
